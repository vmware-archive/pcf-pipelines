---
platform: linux

image_resource:
  type: docker-image
  source: {repository: cloudfoundry/cflinuxfs2}

inputs:
  - name: pcf-pipelines

outputs:
  - name: opsmgr-release-version

params:
  OPSMAN_RELEASE_NAME:
  OPSMAN_RELEASE_TAG:

run:
  path: pcf-pipelines/tasks/create-release-version/task.sh
