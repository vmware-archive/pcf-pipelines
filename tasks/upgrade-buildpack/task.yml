---
platform: linux

image_resource:
  type: docker-image
  source: {repository: cloudfoundry/cflinuxfs2}

inputs:
- name: cf-cli
- name: buildpack
- name: pcf-pipelines

params:
  CF_API_URI:
  CF_ORG:
  CF_USERNAME:
  CF_PASSWORD:
  CF_SPACE:
  BUILDPACK_NAME:

run:
  path: pcf-pipelines/upgrade-buildpack/task.sh
