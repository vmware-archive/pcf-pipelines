---
platform: linux

image_resource:
  type: docker-image
  source: {repository: cloudfoundry/cflinuxfs2}

inputs:
  - name: pcf-pipelines
  - name: diagnostic-report

outputs:
  - name: stemcells

params:
  IAAS_TYPE:
  API_TOKEN:

run:
  path: pcf-pipelines/tasks/download-pivnet-stemcells/task.sh
