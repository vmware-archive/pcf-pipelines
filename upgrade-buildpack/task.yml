---
platform: linux

image_resource:
  type: docker-image
  source: {repository: cloudfoundry/cflinuxfs2}

inputs:
  - name: java-buildpack-latest

params:
  PCF_API_URI:
  PCF_ORG:
  PCF_USERNAME:
  PCF_PASSWORD:
  PCF_SPACE:
run:
  path: concourse-tasks-bundle/import-opsmgr-settings/task.sh
