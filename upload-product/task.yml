---
platform: linux

image_resource:
  type: docker-image
  source: {repository: cloudfoundry/cflinuxfs2}

inputs:
  - name: concourse-tasks-bundle
  - name: tool-om
  - name: pivnet-elastic-runtime

params:
  OPSMAN_USERNAME:
  OPSMAN_PASSWORD:
  OPSMAN_URI:

run:
  path: concourse-tasks-bundle/stage-product/task.sh
